V2.0.1 TEMPLATE-ID-GUARD

场景：同一个交友软件公共页面模板，头像区域可能只是数字/字母/系统占位图。
这类占位图会被多人共用，因此不能把“同一个数字8”当成同一客户。

规则：
1. 普通照片仍以图片同图识别为核心。
2. 对模板页，OCR 读取 HeyID 等稳定账号ID，仅用于排除误撞。
3. 两张模板页 HeyID 明显不同 -> 不撞客。
4. 两张模板页都是低信息占位头像，而无法确认同一账号ID -> 模板/占位头像不能单独触发疑似或自动撞客。
5. HeyID 相同也不会单独判撞客，仍需图片证据。
